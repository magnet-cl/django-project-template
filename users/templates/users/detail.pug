extends base.pug

block content
  div.detail
    div.row
      div.col-md-7
        table.table.table-striped.table-hover.table-bordered
          tr
            th Nombre
            td {{user.first_name}}
          tr
            th Apellido
            td {{user.last_name}}
          tr
            th Correo Electrónico
            td {{user.email}}
        div.text-right
          .btn-group
            a.btn.btn-primary(href="{% url 'user_edit' %}")
              | Editar
