- load i18n

{% url url_name as url %}
{% with current=request.path == url %}
li.nav-item(class="{% if current %}active{% endif %}")
  a.nav-link(href="{{ url }}")
    | {% trans verbose_name|capfirst %}
    if current
      span.sr-only ({% trans 'current' %})
{% endwith %}
