- load i18n

if not url
  {% url url_name as url %}

li.nav-item(class="{% if request.path == url %}active{% endif %}")
  a.nav-link(href="{{ url }}")
    {% trans text|capfirst %}
    if request.path == url
      span.sr-only ({% trans 'current' %})
